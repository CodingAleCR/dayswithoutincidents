name: data
description: Data package
version: 1.0.0
homepage: null
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  domain:
    path: ../domain
  # Value equality
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  # Monitoring
  rxdart: ^0.28.0
  sentry_flutter: ^7.20.2
  # Local cache
  shared_preferences: ^2.2.2
  # Database
  sqflite: ^2.3.0
  # UUID
  uuid: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0
flutter: null
