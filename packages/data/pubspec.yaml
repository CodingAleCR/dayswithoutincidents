name: data
description: Data package
version: 1.0.0
homepage: null
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  domain:
    path: ../domain
  drift: ^2.19.1
  drift_flutter: ^0.1.0
  drift_sqflite: ^2.0.1
  # Value equality
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  # Monitoring
  rxdart: ^0.28.0
  sentry_flutter: ^7.20.2
  # Local cache
  shared_preferences: ^2.2.2
  # Database
  sqflite: ^2.3.0
  # UUID
  uuid: ^4.2.1

dev_dependencies:
  build_runner: ^2.4.11
  drift_dev: ^2.19.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0
flutter: null
