name: data
description: Data package
version: 1.0.0
author:
homepage:
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  domain:
    path: ../domain
  # Value equality
  equatable: ^2.0.3
  # Local cache
  shared_preferences: ^2.0.6
  # Database
  sqflite: ^2.0.0+3
  # UUID
  uuid: ^3.0.4
  # Monitoring
  sentry_flutter: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
